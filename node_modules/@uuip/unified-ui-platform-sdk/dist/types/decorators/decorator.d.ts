import * as debounce from "./decorator-debounce";
import * as evt from "./decorator-evt";
import * as exec from "./decorator-exec";
import * as handle from "./decorator-handle";
import * as once from "./decorator-once";
import * as throttle from "./decorator-throttle";
export declare namespace Decorator {
    export import Debounce = debounce.Debounce;
    export import Evt = evt.evt;
    export import Exec = exec.Exec;
    export import Handle = handle.Handle;
    export import Once = once.Once;
    export import Throttle = throttle.Throttle;
}
