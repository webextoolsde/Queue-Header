/**
 * Transforms any value into an Error. Can be used to ensure that a value is an Error
 * inside a catch block.
 * @param value
 * @returns
 */
export declare function ensureError(value: unknown): Error;
