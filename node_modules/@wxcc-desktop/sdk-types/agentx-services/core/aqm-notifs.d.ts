import { Signal } from "@uuip/unified-ui-platform-sdk";
export declare const extractLogDetails: (eventText: string) => [string | null, object];
export declare class AqmNotifs {
    readonly onMessage: Signal.WithData<string>;
    private readonly onMessageSend;
    readonly onSocketClose: Signal.Empty;
    private readonly onSocketCloseSend;
    private websocket;
    private readonly welcomeTimeout;
    private readonly notifsWelcomeFlag;
    private readonly isUsingReachableEndpoint;
    private readonly useOptimizedConnectionChecks;
    private url;
    private readonly keepaliveWorker;
    private readonly webSocketStatus;
    constructor();
    isSocketClosed(): boolean;
    setShouldReconnect(value: boolean): void;
    private buildEventName;
    private readonly logTrace;
    private readonly logDebug;
    private readonly logError;
    private workerMessageHandler;
    init(): Promise<void>;
    private readonly handleConnectionLost;
    private dispatchEventForRTD;
    private readonly trackWebSocketDisconnect;
    close(shouldReconnect: boolean, reasonValue?: string): void;
    private register;
    webSocketOnCloseHandler: (event: any) => Promise<void>;
    private connect;
}
//# sourceMappingURL=aqm-notifs.d.ts.map