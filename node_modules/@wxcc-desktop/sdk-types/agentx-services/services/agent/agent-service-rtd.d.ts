import { AgentGlobalStatusSubscription, RealTimeCallback } from "./agent-service.types";
/**
 * Handles real-time data updates for agents from wxcc rtd
 */
export declare class AgentServiceRtd {
    /**
     * Subscribes to agent global status updates
     * @param callback Function to call when updates are received
     * @returns Subscription object
     */
    subscribeAgentGlobalStatusUpdates(callback: RealTimeCallback): Promise<AgentGlobalStatusSubscription>;
    /**
     * Handles agent status updates
     * @param updateType Type of update (SubscriptionResponse or RealtimeFeed)
     * @param fullData Complete data set (for initial response)
     * @param deltaData Delta data set (for real-time updates)
     * @returns Agent status object
     */
    private handleAgentStatusUpdate;
    /**
     * Maps rtd data to agent
     * @param dataSetValue Agent global status from rtd
     * @returns Mapped agent
     */
    private mapRtdDataToAgent;
}
//# sourceMappingURL=agent-service-rtd.d.ts.map