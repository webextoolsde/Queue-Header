import { NotifsWebsocketFactory } from "./aqm-service-notifs-websocket-factory";
export declare class ServiceConnectionForWorker {
    private readonly websocketFactory;
    private websocket?;
    private randomizedIntervalsCounter;
    private reconnectInterval?;
    private reconnectionStartTime?;
    private shouldReconnect;
    private autoResolveAfterDelay;
    constructor(websocketFactory: NotifsWebsocketFactory);
    start(autoResolveAfterDelay: boolean): Promise<void>;
    private tryReconnect;
    private reconnectWithExponentialDelay;
    send(message: any): void;
    closeWebsocket(reason: string, shouldReconnect?: boolean): void;
}
//# sourceMappingURL=aqm-service-connection-for-worker.d.ts.map