import { MicrosoftService } from "../integration/microsoft/microsoft-authentication";
import { WebexService } from "../webex/webex-service";
export declare class AvatarService {
    private readonly webexService;
    private readonly microsoftService;
    private readonly cache;
    private pendingEmails;
    private requestingAvatars;
    private dssRetries;
    constructor(webexService: WebexService, microsoftService: MicrosoftService);
    init(): void;
    getAvatarUrl(email: string): Promise<string | undefined>;
    private findCacheItem;
    private requestAvatarUrl;
    private readonly requestWebexAvatarUrls;
    private resolvePromises;
    private rejectPromises;
    private requestWebexAvatarUrlsBatch;
}
//# sourceMappingURL=avatar-service.d.ts.map