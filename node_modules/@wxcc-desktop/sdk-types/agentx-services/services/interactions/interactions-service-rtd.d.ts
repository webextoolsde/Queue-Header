import { InteractionsServiceTypes } from "./interactions-service.types";
/**
 * Handles real-time data interaction updates from WebexCC RTD
 */
export declare class InteractionsRtdEndpoint {
    /**
     * Subscribes to interaction updates
     * @param queryType Type of interactions to subscribe to (active or queued)
     * @param callback Function to call when updates are received
     * @returns Subscription object
     */
    subscribeInteractionUpdates(queryType: InteractionsServiceTypes.ActiveOrQueuedInteractions, callback: InteractionsServiceTypes.RealTimeCallback): Promise<InteractionsServiceTypes.ContactStatsSubscription>;
    /**
     * Maps RTD data to task details
     * @param dataSetValue Agent contact stats from RTD
     * @returns Mapped task details
     */
    private mapRtdDataToTaskDetails;
    /**
     * Handles interaction updates (both active and queued)
     * @param updateType Type of update
     * @param fullData Complete dataset (for initial responses)
     * @param deltaData Delta dataset (for realtime updates)
     * @returns Formatted interaction update object
     */
    private handleInteractionUpdate;
    private handleActiveInteractionUpdate;
    private handleQueuedInteractionUpdate;
}
//# sourceMappingURL=interactions-service-rtd.d.ts.map