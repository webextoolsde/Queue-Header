export type MessageHandler = ((ev: MessageEvent) => any) | null;
export type ErrorHandler = ((ev: ErrorEvent) => any) | null;
/**
 * Creates a worker with the given filepath and messageHandler. Logs are already configured
 * to be sent to the logger.
 * @short Creates a worker with the given filepath and messageHandler
 * @param filepath
 * @param messageHandler
 */
export declare function createWorker(filepath: string, messageHandler?: MessageHandler, errorHandler?: ErrorHandler): Worker;
//# sourceMappingURL=workerUtil.d.ts.map