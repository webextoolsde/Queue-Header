import { LitElement } from "lit-element";
export declare namespace CssDynamic {
    type TestVars = {
        "--some-example": string;
    };
    type InteractionControlVars = {
        "--interaction-control-block-height": string;
    };
    type AgentXVars = Partial<TestVars & InteractionControlVars>;
    class Element extends LitElement {
        vars: AgentXVars;
        updated(): void;
        private get strVars();
        createRenderRoot(): this;
        render(): import("lit-element").TemplateResult;
    }
}
