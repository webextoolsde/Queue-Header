/// <reference types="node" />
import "@agentx/agentx-mfe-wc-based";
import "@uuip/unified-ui-platform";
import { LitElement } from "lit-element";
/**
 * @element page-title
 */
export declare class PageTitle extends LitElement {
    pageTitle: string;
    titleText: string;
    _timerInterval: NodeJS.Timeout;
    titleHasRegex: (title: string) => boolean;
    extractString: (regexString: string) => string;
    static get styles(): import("lit-element").CSSResult;
    extractPageTitle: (title: any) => any;
    connectedCallback(): void;
    disconnectedCallback(): void;
    render(): import("lit-element").TemplateResult;
}
