import { Router } from "@uuip/unified-ui-platform";
export interface BaseViewOptions {
    taskArea?: boolean;
}
export declare const BaseView: (opts?: BaseViewOptions) => Router.RouterView.BlocksMap;
