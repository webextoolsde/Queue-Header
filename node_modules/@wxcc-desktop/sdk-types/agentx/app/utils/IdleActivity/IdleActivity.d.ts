export declare class IdleActivity {
    timeout: number;
    private readonly interval;
    private readonly eventList;
    private readonly signOutTimer;
    private countDownTimerId;
    constructor(interval: number);
    /**
     * This clears out the old timeout and start a new setTimeout.
     * STORE is referred to identify if any active task exist.
     */
    handleInActivityTimer: () => void;
    /**
     * Singout handling modal
     */
    handleSignoutClick(): Promise<void>;
    /**
     * for initiating initial modal.
     */
    showWarningModal(): void;
    /**
     * agent signed out and info modal and logout on ok.
     */
    inactivityTimeOutAgentLogout(): Promise<void>;
    /**
     * Register all UI related events here.
     */
    register(): void;
    /**
     * postMessage listener from Iframe based widgets
     */
    postMessageListener(): void;
    eventListener: (e: any) => void;
    deRegister(): void;
}
