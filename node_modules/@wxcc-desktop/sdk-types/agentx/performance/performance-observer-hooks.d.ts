/**
 As this has to be the first thing that takes places in the application, before any UI is instantiated, the feature
 flag must be picked up from localStorage. As a result, setting the feature flag will require 2 restarts to take effect -
 the first time, the value will be stored, allowing it to be picked up the second time.
 */
export declare function observePerformance(): Promise<void>;
