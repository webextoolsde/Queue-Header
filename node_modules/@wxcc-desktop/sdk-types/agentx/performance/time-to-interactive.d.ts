type TimeToAppInteractiveTiming = {
    duration: number;
    interactiveStep: boolean;
};
export declare function retrieveTimings(names: string[]): Map<string, TimeToAppInteractiveTiming>;
export declare function storeTiming(name: string, timing: TimeToAppInteractiveTiming): void;
export {};
