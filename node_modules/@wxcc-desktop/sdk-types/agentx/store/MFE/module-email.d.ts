import { EmailComposer } from "@agentx/agentx-mfe-react-based";
import { Service } from "@agentx/agentx-services";
export default class ModuleEmail {
    private readonly logger;
    private _template;
    private readonly _emailCache;
    constructor();
    /**
     * Computed
     */
    get templateSelector(): Service.Emm.EmailTemplate;
    get emailCacheSelector(): Record<string, EmailComposer.CachedEmailContent>;
    /**
     * Actions
     */
    setTemplate: (e: EmailComposer.EEmailTemplateFetched) => void;
    updateDraftEmailContent: (e: EmailComposer.ECachedEmailContent) => void;
    deleteEmailFromCache: (interactionId: string) => void;
}
