import { Store } from "@/store";
import { ErrorModal } from "@uuip/unified-ui-platform-common-components";
export default class ModuleErrorDialog {
    errorDetails: ErrorModal.Detail;
    isModalOpen: boolean;
    errorDetailsList: ErrorModal.Detail[];
    constructor();
    get localization(): ErrorModal.Localization;
    /**
     * Actions
     */
    private shiftErrorDetail;
    setErrorDetails<T extends Store.Session.ModuleErrorDialog.EErrorDetail>(e: T): void;
    updateModalStatus(e: boolean): void;
}
declare module "@/store" {
    namespace Store.Session.ModuleErrorDialog {
        type EErrorDetail = {
            detail: ErrorModal.Detail;
        };
    }
}
