import { ScreenPop } from "@agentx/agentx-mfe-wc-based";
import { Service } from "@agentx/agentx-services";
export declare const SCREEN_POP_CONFIG = "screenPopConfig";
export default class ModuleScreenPop {
    private screenPopUrl;
    constructor();
    /**
     * Computed
     */
    get screenPopSelector(): Array<ScreenPop.ScreenPopConfig>;
    /**
     * Actions
     */
    private readonly setScreenPopUrl;
    updateScreenPopData: (interactionId: string) => void;
    private readonly getTaskDetils;
    private readonly getScreenNotificationConf;
    private getMediaTypeIcon;
    /**   This Method is nowhere used or called
     * @action addSilentNotificationForScreenPop = (finalUrl: string, interactionId: string) => {
     *   STORE.generalNotifications.fireScreenPop(
     *     this.getScreenNotificationConf(finalUrl, interactionId, screenPopDesktopLabel)
     *   );
     * };
     */
    addToastNotificationForScreenPop: (finalUrl: string, interactionId: string, screenPopDesktopLabel: string) => void;
    /** update the screen pop config in the store so that <agentx-wx-screen-pop> should be rendered*/
    updateScreenPopStore: (screenPop: Array<ScreenPop.ScreenPopConfig>) => void;
    updateScreenPopStoreOnReload: () => void;
    handleScreenPop: (screenPopDetail: Service.Aqm.ScreenPop.ScreenPopDetail) => void;
}
