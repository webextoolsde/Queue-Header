import { AsyncSharedAction } from "@/store/shared-actions/types";
import { ModuleAuth } from "../store-auth";
export declare namespace AuthSharedActions {
    namespace GetToken {
        type AccessToken = ModuleAuth["accessToken"];
        type ReqPayloadParameters = [AsyncSharedAction.ReqUUID?];
        type ResPayloadParameters = [AccessToken, AsyncSharedAction.ResUUID?];
        type ReqActionName = "getToken/Req";
        type ResActionName = "getToken/Res";
    }
    const Init: () => void;
}
