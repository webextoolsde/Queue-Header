import { AsyncSharedAction } from "@/store/shared-actions/types";
import { Notifications } from "@uuip/unified-ui-platform-sdk";
export declare namespace GeneralNotificationsSharedActions {
    namespace FireSilent {
        type ReqPayloadParameters = [
            Notifications.ItemMeta.Raw & {
                data: {
                    mode: Notifications.ItemMeta.Mode.Silent;
                    data: string;
                };
            }
        ];
        type ReqActionName = "fireGeneralSilentNotification/Req";
    }
    namespace FireAutoDismiss {
        type ReqPayloadParameters = [
            Notifications.ItemMeta.Raw & {
                data: {
                    mode: Notifications.ItemMeta.Mode.AutoDismiss;
                };
            },
            AsyncSharedAction.ReqUUID?
        ];
        type ResPayloadParameters = [
            Notifications.ItemMeta.Status,
            Notifications.ItemMeta.StatusChangeEventReason,
            Notifications.ItemMeta.Mode,
            AsyncSharedAction.ResUUID?
        ];
        type ReqActionName = "fireGeneralAutoDismissNotification/Req";
        type ResActionName = "fireGeneralAutoDismissNotification/Res";
    }
    namespace FireAcknowledge {
        type ReqPayloadParameters = [
            Notifications.ItemMeta.Raw & {
                data: {
                    mode: Notifications.ItemMeta.Mode.Acknowledge;
                };
            },
            AsyncSharedAction.ReqUUID?
        ];
        type ResPayloadParameters = [
            Notifications.ItemMeta.Status,
            Notifications.ItemMeta.StatusChangeEventReason,
            Notifications.ItemMeta.Mode,
            AsyncSharedAction.ResUUID?
        ];
        type ReqActionName = "fireGeneralAcknowledgeNotification/Req";
        type ResActionName = "fireGeneralAcknowledgeNotification/Res";
    }
    const Init: () => void;
}
