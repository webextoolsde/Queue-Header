/**
 * store-common-state-variables store is used to share common variables/state accross components
 */
export declare class CommonState {
    navBarOpen: boolean;
    constructor();
    /**
     * Update Modal State Open or Close
     */
    updateIsNavBarOpen(navBarOpen: boolean): void;
    get isNavBarOpen(): boolean;
    /**
     * ref CX-5228
     * 1. taskbar and incoming popover will be on top of modal overlay if CONSULT/CONFERENCE/TRANSFER model is open to prevent RONA
     * 2. task switching will be disabled
     *
     * interactionModalState is set to TRUE if interaction control modal is open
     */
    interactionModalState: {
        interactionID: null;
        state: boolean;
    };
    updateInteractionModalState(interactionModalState: any): void;
    deleteInteractionModalState(): void;
    get interactionModelOpenId(): null;
    get isInteractionModelOpen(): boolean;
}
declare module "@/store" {
    namespace Store.CommonState { }
}
export declare const common: CommonState;
